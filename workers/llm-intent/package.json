{
  "name": "@handsoff/llm-intent-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@handsoff/contracts": "workspace:*",
    "@handsoff/intent": "workspace:*",
    "openai": "^6.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260621.1",
    "wrangler": "^4.103.0"
  }
}
