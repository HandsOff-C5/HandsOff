{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the HandsOff dashboard window.",
  "windows": ["main", "overlay"],
  "permissions": ["core:default"]
}
